<ul class="nav navbar-nav navbar-right">
    <li>
        @if (Session["CartCounter"] == null)
        {
            @Html.ActionLink("Koszyk", "ShoppingCart", "Shopping", new {id = "cartItem"})
        }
        else
        {
            @Html.ActionLink("Koszyk(" + Session["CartCounter"] + ")", "ShoppingCart", "Shopping", new { id = "cartItem" })
        }
        
    </li>
</ul>
